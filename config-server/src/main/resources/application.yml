server:
  port: 9192

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server: 
        git: 
          uri: https://github.com/sachindra-maharjan/config-server
          clone-on-start: true
          repos:
            gitpod:
              pattern: gitpod*
              uri: file://workspace/SpringBoot-Micriservice-Architecture/config-service-configuration/gitpod
        
